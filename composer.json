{
    "name": "dilawar/nested-json-flattener",
    "description": "A php package to flatten nested json objects and nested arrays. It also allows you to create csv files from the flattened data.",
    "keywords": ["json", "nested json", "Flatten nested json", "json flattener", "convert json to csv", "csv", "csv writer"],
    "license": "MIT",
    "authors": [
        {
            "name": "tonirilix",
            "email": "jesusmontes.mx@hotmail.com"
        },
        {
            "name": "Dilawar Singh",
            "email": "dilawar_s@zohomail.in"
        }
    ],
    "autoload": {
        "psr-4": {
            "NestedJsonFlattener\\": "src/"
        }
    },    
    "scripts": {        
        "test": "./vendor/bin/phpunit --configuration ./phpunit.xml.dist"
    },
    "require": {
        "php": ">7.0",
        "peekmo/jsonpath": "dev-master"
    },
    "prefer-stable": true,
    "require-dev": {
        "phpunit/phpunit": "^12.0",
        "phpstan/phpstan": "^2.1"
    }
}
